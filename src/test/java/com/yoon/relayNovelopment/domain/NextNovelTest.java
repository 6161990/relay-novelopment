package com.yoon.relayNovelopment.domain;

import org.junit.jupiter.api.Test;

public class NextNovelTest {

    // 좋아요가 카운팅될 수 있다.
    // 댓글을 달 수 있다.
    // 상태가 변이된다 APPLIED, PUBLISH , REJECT, 강등
    // isRelay relayting 이 되었느냐 (노드가 달렸느냐)

    @Test
    void isRelay() {
    }
}
